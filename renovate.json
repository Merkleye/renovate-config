{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "This repo's own config. Deliberately empty of policy -- org-inherited-config.json already applies here through inheritance. Its only job is to keep Renovate from opening an onboarding PR against this repo; inheritance itself does not require this file."
}
